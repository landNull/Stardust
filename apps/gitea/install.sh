#!/bin/sh
# modules/60-gitea.sh — optional Gitea prompts only
# Do not download a binary from the Gitea homepage.
# Do not rewrite an existing app.ini.

echo "STEP 60: Gitea defaults (prompt only)"

maybe_gitea_defaults
